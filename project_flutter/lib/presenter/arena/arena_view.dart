import 'package:flutter/material.dart';

class ArenaView extends StatelessWidget {
  const ArenaView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('eu sou a arena'));
  }
}